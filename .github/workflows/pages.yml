name: Site

# Builds `site/` and deploys it to GitHub Pages. No `gh-pages` branch:
# Pages can publish a workflow artifact, so the site's source lives
# beside the code it describes and cannot drift onto a branch nobody
# looks at.
#
# Requires Settings → Pages → Source = "GitHub Actions", once.

on:
  push:
    branches: [main]
    paths:
      - "site/**"
      - "scripts/site/**"
      - ".github/workflows/pages.yml"
      # Four pages are rendered from the repository rather than written
      # in site/content, so a change to any of these changes the site
      # without touching site/: the rules page comes from the gripes
      # catalogue, the roadmap page from roadmap.md, the release notes
      # from the changelog, and the adapters table from the contracts.
      # Leaving these out means shipping a rule the website denies.
      - "roadmap.md"
      - "CHANGELOG.md"
      - "packages/gripes/**"
      - "packages/contracts/src/adapters.ts"
  # Called by release.yml, so a release always lands a fresh site
  # whatever the commit behind it touched — the documentation describes
  # the version people can now install.
  #
  # Called rather than triggered on `release: published`, because that
  # release is published by a workflow using GITHUB_TOKEN and GitHub
  # does not let a token-created event start another run. The trigger
  # would look right and never fire.
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# A queued deploy should replace a pending one, but never cancel a run
# that is mid-deploy.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.4.0"

      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts

      # Renders the site into site/dist, and refuses to publish a
      # broken internal link, an unparseable roadmap line, or a rule
      # listed as planned that has already shipped. See site/README.md.
      - name: Build the site
        run: bun run build:site

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site/dist

      - id: deploy
        uses: actions/deploy-pages@v4
